import type { ExampleConfig, ParsedBindings } from '../types';
export declare function vanillaToReactFunctionalTs(bindings: ParsedBindings, exampleConfig: ExampleConfig, componentFilenames: string[], allStylesheets: string[]): () => string;
