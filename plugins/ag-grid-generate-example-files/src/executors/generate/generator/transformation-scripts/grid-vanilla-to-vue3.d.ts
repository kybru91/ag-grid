import type { ExampleConfig, ParsedBindings } from '../types';
export declare function vanillaToVue3(bindings: ParsedBindings, exampleConfig: ExampleConfig, componentFileNames: string[], allStylesheets: string[]): () => string;
