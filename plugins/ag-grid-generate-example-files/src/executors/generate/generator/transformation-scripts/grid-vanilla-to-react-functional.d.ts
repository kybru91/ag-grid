import type { ExampleConfig, ParsedBindings } from '../types';
export declare function vanillaToReactFunctional(bindings: ParsedBindings, exampleConfig: ExampleConfig, componentFilenames: string[], allStylesheets: string[]): () => string;
